import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'move.a.block',
  appName: 'move-a-block',
  webDir: 'dist/move-a-block',
  bundledWebRuntime: false,
};

export default config;
